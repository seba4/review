Usefull commands:


Ionic help:
https://ionicframework.com/docs/cli/commands/build

ionic build
ionic build --prod

Rebuilds when files change.
ionic build --watch


Run with docker:
docker-compose build
docker-compose up

Open: http://localhost
